import React, { useState } from "react";

export default function useTheme() {
  const [hookTheme, setHookTheme] = useState();

  // STORE CURRENT THEME IN HOOK
    
  return <div>
  </div>;
}
